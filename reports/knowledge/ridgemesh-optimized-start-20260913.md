# RidgeMesh optimized start — 2026-09-13

Use the [release record](../../docs/OPTIMIZED-START-RELEASE-2026-09-13.md) and [WBS](../../docs/OPTIMIZED-START-WBS.md) for source, acceptance and deployment evidence. Codex memory `project_ridgemesh_recovery.md` and the public-reference Graphiti episode point to this release.

Decision: ship a reproducibly generated applied router plan, keep initial visual detail minimal, and reveal editing/layers explicitly. The real base stays distinct from the forwarding router; hidden handhelds stay in the RF calculation. The generated plan must match the actual optimizer's current site/height policy and 40 m area targets.

Gotchas: preserve deliberate empty/custom plans; back up an exact old starter before upgrading; preserve unreadable bytes before autosaving; cancel and invalidate in-flight worker responses when leaving customization. Test browser workflows on z370 with the current 22-check harness and normalize only scoped screenshots before viewing.
