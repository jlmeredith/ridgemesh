# Optimized starting plan and progressive detail — 2026-09-13

**Phase (SoT):** [#ridgemesh·P3] Optimized start → simple planner
**Status:** Remote acceptance passed; production replacement pending.
**Baseline:** Documentation/harness `3557decbb3e5896c28b15c3f7ee8defe72b9e284`; preceding production application `2ddbc862b48415a3f2d98900bd73810f2b552436`.

## Delivered behavior

Fresh visits now load an applied optimized network with one V4 forwarding router at Stargazer and six illustrative M1 handhelds, retaining the Main Stage handheld as the base. The first view shows a clear aerial, distinct Base B and Router R1 markers, router site/antenna height, handheld count and estimated strong coverage within event-use areas. The crew remains in the calculations while its markers are hidden. This router forwards the radio mesh; it is not an internet gateway.

Customize reveals the roster and device editing. Coverage settings/results, equipment comparisons and path/mast inspection remain collapsed until opened. Layers independently adds handhelds, place labels, antenna heights, event areas/roads, routes, contours, coordinates and coverage. Imagery/date selection, synchronized comparison and 3D sit within an optional imagery/terrain disclosure. Basic inspection cannot accidentally move or delete a device. Leaving Customize cancels active proposals and rejects late worker replies, so a closed preview cannot reappear.

Existing custom and intentionally empty scenarios are retained. Only the exact former autogenerated six-handheld scaffold upgrades automatically, with its original bytes backed up first. Plan → Load optimized plan and imports preserve the complete previous scenario; Restore previous plan switches back. Unreadable or incompatible saved contents are copied before autosave may replace them. When that backup cannot be written, autosave is blocked and the original bytes remain untouched; an export remains available. Layer visibility is presentation state and does not change the network model.

## Reproducible starting plan

[Generated scenario and primary search evidence](../public/data/optimized-plan.json): `astral-optimized-start-v1`, site `astral-source-reconciliation-20260913-v1`, terrain `usgs-2021-10m-v1`, RF `ridgemesh-rf-3.0`, scenario schema 2. `scripts/generate-starter-plan.ts` runs only on z370 and shares `src/lib/placement-policy.ts` with the interactive search. An integration test reruns the actual optimizer and checks its nodes, base, bidirectional router path and area statistics against the committed scenario.

**RAN ON z370 · optimized-start-generation-20260913:** 45 site/height candidates, budget two additional devices, 12 m height cap, 132 layouts evaluated. Best found adds one Stargazer router at 1.5 m AGL. On the same 40 m event-area samples, the weighted score rises from 11.36 to 11.68 ha-equivalent, with 11.68 ha of modeled target area and 100% classified strong. The objective prefers fewer relays, then lower sufficient heights when scores tie. This is a bounded search result under editable assumptions, not a proof of global optimality or coverage over the entire property.

Terrain, imagery, source registration, RF physics and source uncertainty remain those of the [preceding correction](PLANNER-CORRECTION-RELEASE-2026-09-13.md). Default crew positions are illustrative; access, mounting, power, canopy and actual packet delivery require field validation. The displayed height is a tested RF assumption, not a structural installation specification.

## Verification and visual evidence

All tests, builds and browser execution ran on **z370** or hosted CI, never the Mac. **z370 · optimized-start-integration-20260913 · 3557dec worktree snapshot:** 47/47 domain, source, scenario and optimized-start integration tests passed. **z370 · optimized-start-release-20260913 · 3557dec worktree snapshot:** TypeScript, ESLint and production build passed; all 22 browser assertions passed, with zero errors and zero failed requests. This run used a temporary remote dev server, not the deployed URL.

Root inspected the [primary browser report](evidence/optimized-start/browser-report.json), including completed HTTP 200 responses and positive-dimension decoding of all three original imagery acquisitions. Tests cover minimal startup, actual router/base roles, independent layers, editing protection, customization, location/height changes, separate mast thresholds, comparisons, bounded search preview/application, stale-response cancellation, persistence/restore, exact legacy upgrade, intentionally empty plans, corrupt/quota recovery and mobile layout.

The screenshot gate returned success without warnings for all seven scoped captures. Root inspected normalized [default aerial](evidence/optimized-start/planner-default.png), [compact overview](evidence/optimized-start/planner-overview.png) and [mobile map](evidence/optimized-start/planner-mobile.png). [Editing controls](evidence/optimized-start/planner-controls.png), [3D](evidence/optimized-start/planner-3d.png) and [proposal](evidence/optimized-start/planner-proposed.png) retain the optional detail flows. Root checked the generated artifact against the primary optimizer test and the final source against the browser assertions; no survey or field RF claim is verified by these software checks.

## Deployment and rollback

Target is the existing [RidgeMesh application](https://ridgemesh.vercel.app), Vercel project `prj_t9DUwu4aWVM1FmPn8sTrHiZALLOP`. [Draft PR #2](https://github.com/jlmeredith/ridgemesh/pull/2) retains the full recovery; main remains unmerged. Exact source, hosted CI, production deployment and canonical browser results will be appended after replacement.

Rollback is preceding production `dpl_3FLar7MJBGq6yb3dEjCq4kNkkzBJ`, revision `2ddbc862b48415a3f2d98900bd73810f2b552436`. It retains corrected geography but uses the earlier crowded handheld-only starting view.

## Durable knowledge

The default is a versioned applied scenario, regenerated with the same search policy and RF targets as customization. A basic map should expose the base/router and one meaningful area metric, with detail under explicit controls. Startup migrations must distinguish an untouched scaffold from deliberate custom or empty data and preserve original bytes before replacement. Leaving a workspace must invalidate its in-flight worker messages, not merely hide its current preview.

The reusable workflow is `.Codex/skills/ridgemesh-remote-verification/SKILL.md`; the current harness is `scripts/optimized-start-browser-check.mjs`. The release is cross-linked from `reports/knowledge/ridgemesh-optimized-start-20260913.md` and Codex memory `project_ridgemesh_recovery.md`. Graphiti receives only a public release-document reference after publication.
