# Planner domain correction — 2026-09-13

Model `ridgemesh-rf-3.0` uses editable receiver antenna AGL (default 1.5 m), per-system selected crew, and the strongest known bottleneck route within the edge budget. Likely, marginal, unavailable and unknown area fractions remain separate; compatibility fractions still include likely plus marginal. Default illustrative crew positions use the reconciled site-feature source points: Main Stage first, followed by the five camp points, all explicit 1.5 m handheld clients. These are planning positions, not observations of deployed radios.

`planningTargets(params, step)` and `computeHeat(..., step)` share cell centers, clipped metric cell weights and the union of inferred site target polygons. User polygon overrides that union. Weights are hectares; unknown cells remain in the denominator. Exact camp results are independently evaluated and must not be mistaken for surveyed camp-area coverage. Source geometry and allowances are recorded in `public/data/site-features.json`.

`planMastHeight(a,b,...)` raises endpoint a only, testing 0.5 m increments from 0.5 m to the cap (maximum 30 m), requiring at least 6 dB directed margin in both directions. It separately returns sampled terrain line-of-sight and 60% first-Fresnel clearance thresholds; the 60% threshold is an explicit planning criterion. Ground-clearance geometry excludes canopy; RF budgets retain scenario foliage. Unknown inputs and an unsuccessful capped search return no required-height number. This is a model search, not a mast design, permission, hardware specification or delivery guarantee.

Placement searches site/height combinations, allows a relay beside a handheld, and prevents duplicate fixed routers at an occupied site. Equal weighted coverage prefers fewer relays then lower total antenna height. Likely area receives full weight and marginal area half weight. The search is bounded at 96 candidate combinations, six additions and 16 site-diverse candidates for paired lookahead; the latter can bridge a beneficial two-relay chain when no single addition improves the objective. It remains a heuristic and can miss other combinations. Existing node heights are held fixed.

Validation: **RAN ON z370**, `/home/jamie/ridgemesh-planner-correction-20260913`, run **ridgemesh-domain-correction-20260913**, Node 24.21.0. Domain radio, optimizer, mast, area and shared-target suites passed **18/18** after the final candidate-cap/shortlist adjustment. Independent checks cover midpoint ridge height (8 m ground obstruction, 1.5 m receiver gives 14.5 m LOS mast threshold), Fresnel geometry, minimum tested bidirectional budget, unknown terrain, power-limited cap, strongest alternate route, receiver height propagation, system-filtered crew, single-add exhaustive oracle, handheld co-location, smallest equal-score mast, paired relay chain, matching polygon denominator and source-aligned handheld baseline. No tests or builds ran on the Mac. Type-checking at this checkpoint found only the root-owned scenario schema migration still in progress; final integrated verification belongs to release evidence.

## Integrated realistic performance and objective consistency

**RAN ON z370**, run `ridgemesh-domain-realism-20260913`, same remote workspace and Node 24.21.0, 120-second process timeout. Actual source-aligned six-handheld default in **base mode** (`m1-0` at Main Stage), receiver AGL 1.5 m, updated interpreted land cover, no client forwarding. Target raster step 8 = 80 m; 19 included cells represent 121,600 m² (12.16 ha). Nine source candidate sites × five available heights (1.5, 3, 6, 9, 12 m) = 45 combinations; budget two added relays.

Baseline analysis: 327.93 ms. Search: 354.56 ms, 159 evaluated layouts. Proposed-layout analysis: 295.69 ms. End-to-end calculation: 978.84 ms. No timeout or cancellation. The independent heat-derived objective `(likelyFraction + 0.5*marginalFraction) × hectares` differs from both optimizer baseline and proposed scores by only `1.78e-15`; both equal 12.16 effective hectares. Proposed score does not worsen baseline.

Under these uncalibrated default radio assumptions the baseline already predicts 100% likely target area, 0% marginal, unavailable or unknown mesh area. All five exact camps are likely: Main Stage Field 75.26 dB, Stargazer 30.41 dB, Harmony Grove 19.62 dB, Dreamcatcher 13.99 dB, Family / quiet camp 24.58 dB minimum bidirectional route margins. The optimizer correctly recommends **zero added relays**, gain zero. Its three returned equal-score illustrative alternatives use 1.5 m antenna heights, while the baseline wins the fewer-devices tie. This is not evidence that physical coverage is guaranteed; it means the current declared model does not establish a relay benefit. Do not manufacture losses or obstacles merely to force a recommendation.

## Final 40 m sampling and bounded target evaluation

This supersedes the coarse 80 m result above. **RAN ON z370**, run `ridgemesh-domain-40m-20260913`, Node 24.21.0, same actual defaults/sites/heights and 120-second timeout. `computeHeat` now initializes excluded cells to unknown with NaN margins and skips their RF calculations; exact site inspection remains independent. Shared default raster step is 4 = 40 m, retaining the same source polygons. It includes **73 cells / 116,800 m² / 11.68 ha**; raster area remains an approximation and changes with cell-center sampling.

Baseline analysis **42.25 ms**, optimization **902.17 ms** for **132 layouts**, proposed analysis **33.03 ms**, total **978.15 ms**. The finer samples expose four marginal cells that the coarse raster missed: baseline **94.52% likely / 5.48% marginal / 0% unavailable / 0% unknown**. The best found single **Stargazer relay at 1.5 m AGL** yields **100% likely** in the sampled target area, improving weighted objective from **11.36 to 11.68 effective hectares (+0.32)**. A 1.5 m Harmony Grove alternative has equal weighted coverage; a 3 m Stargazer alternative has no extra scored benefit. All five exact camp points remain likely before/after. Independent heat-derived scores match optimizer values within **7.11e-15**, so the apparent improvement uses identical target samples/weights rather than different baseline sampling. No physics assumptions, source coordinates or losses were adjusted to produce this result.

The domain regression suite now additionally covers 40 m populated-cell membership, unevaluated outside-target values and accidental POI metadata. Receiver construction whitelists coordinates/label and sanctioned receiver settings; an extra `kind: "camp"`, `txDbm`, or `configurationKnown` property on a clicked site cannot become radio hardware or override RF configuration. Final domain suites passed **20/20 on z370**, run `ridgemesh-domain-40m-20260913` (427.10 ms total runner duration); browser/build integration remains recorded by release validation.
